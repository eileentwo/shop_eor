<button class="btn" bindtap="{{disabled ? '' : 'onAddressTap'}}" type="button" open-type="{{(!gps && !disabled)  ? 'openSetting' : ''}}" bindopensetting="{{disabled ? '' : 'getAddress'}}">
  <slot></slot>
</button>