/**
 * 删除首位逗号
 */
var delDotted = function (str = '') {
  str = str.substring(1, str.length - 1);
  return str;
}

// var dotted = function (t) {
//   var tt = t.split(',').join('')
//   return tt;
// }

module.exports = {
  delDotted: delDotted
}